# 授权范围

## 什么算「已授权场景」

用户在本次会话中显式给出路径的场景。

## 什么不算

- 用户提到但没给路径的场景
- 通过环境变量或配置推断出来的「最近打开」场景
- 扫描目录找到的场景

## 为什么严格

Maya 场景常含未公开的项目内容。检查技能虽然只读，但回执会流向
`codex-dreamina-3d` 的编排日志。因此范围必须由用户显式指定。

## 实现位置

授权判断不在本技能里，而在调用方（Codex 会话）。本技能信任传入的
`scene_path` 已获授权，并对其做只读处理。
