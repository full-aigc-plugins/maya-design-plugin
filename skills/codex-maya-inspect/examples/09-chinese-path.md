# 示例 9：中文路径场景

**用户说：** 「我的场景路径里有中文，能读吗？」

**执行：** `codex-maya-inspect`，`scene_path = 场景/主角.ma`

**输出要点：**
- 中文路径是支持的
- argv 数组调用，不做 shell 拼接，空格与中文都不会被重新分词
- 若仍报错，交给 `codex-maya-diagnose` 区分是编码问题还是模块问题
