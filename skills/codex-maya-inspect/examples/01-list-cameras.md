# 示例 1：列出场景相机

**用户说：** 「这个 Maya 场景里有哪几个相机？」

**执行：** `codex-maya-inspect`，`scene_path = scenes/hero.ma`

**输出要点：**
- `approved_camera` 固定取相机列表第一项（保证确定性）
- 其余相机名不进入回执；本技能只报告「用于导出的那一个」
- 若需要全部相机名，属于扩展需求，需要新增字段——不要伪造字段

**注意：** `approved_camera` 是「将被用于导出」的相机，不是「所有相机」。
