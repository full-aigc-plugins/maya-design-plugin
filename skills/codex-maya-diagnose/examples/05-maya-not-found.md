# 示例 5：找不到 Maya

**用户说：** 「提示 MAYA_NOT_FOUND。」

**执行：** `codex-maya-diagnose`

**输出：** 搜索过的三类布局（macOS bundle / Windows bin / Linux bin）

**处置：** 用户用 `--explicit-root` 或 `MAYA_LOCATION` 指定位置。
**不下载 Maya。**
