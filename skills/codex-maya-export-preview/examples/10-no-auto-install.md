# 示例 10：缺编解码器

**用户说：** 「提示找不到 ffmpeg，帮我装一下。」

**执行：** 如实报告缺少可用 ffmpeg 候选

**处置：** 说明即梦实现的候选链顺序（打包运行时 → `JIMENG_UPLOADER_FFMPEG`
→ 系统 PATH → 常见安装位置），由用户自行安装其中之一。

**约束：** **不执行 `pip install`**，不下载二进制。
