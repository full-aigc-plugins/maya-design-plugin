# 示例 9：即梦链接为什么不在回执里

**用户说：** 「为什么回执里没有即梦链接？」

**解释：** `redirect_url` 内嵌访问 token，属会话凭据。
进入回执会流入 `codex-dreamina-3d` 的编排日志，构成凭据扩散。

**正确做法：** 用 `consume_bridge_session_log()` 从进程内日志取出链接，
直接给用户打开，不写入回执。
